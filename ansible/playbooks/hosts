# Machine responsible for generating GPG key (Only First machine will be used)
[gpg]

# OpenShift Master to execute cluster commands
[masters]

#All OpenShift Nodes in the cluster
[nodes]

# Dedicated OpenShift nodes for managing images and related infrastructure components
[image_managers]
